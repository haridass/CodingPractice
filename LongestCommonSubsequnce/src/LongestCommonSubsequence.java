/*********************************************************
 * The main observation is that the longest subsequence should be increasing in both arrays
 * prepare an array of indices with index pointing other indices named indices
 * opt[i]= MAX(opt[j] +1,1) such that array1[j] < array1[i] and indices[j] < indices[i]
 * @author sriram
 *
 */
public class LongestCommonSubsequence {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
